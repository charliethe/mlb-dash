services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - ODDS_API_KEY=${ODDS_API_KEY:?ODDS_API_KEY is required}
      - API_KEY=${API_KEY:-}
    volumes:
      - ./backend/data:/app/data
    restart: unless-stopped
